services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Необязательный серверный ключ OpenRouter (см. .env.example).
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
    restart: unless-stopped
