# Все параметры необязательны — приложение запускается со значениями по умолчанию.

# Разрешённые источники CORS (JSON-массив).
# CORS_ORIGINS=["*"]

# Время жизни кэша результатов поиска, секунды.
# CACHE_TTL=600
